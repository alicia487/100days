from question_model import Question
from data import question_data


question = Question()

def question_bank(self, q_text, q_answer):
    questions = []
    for data in question_data:
        question_text = self.q_text["text"]
        question_answer = self.q_answer["answer"]



question_bank(data,"text","answer")